machine:
  ruby:
    version:
      2.4.2

database:
  pre:
    - cp config/application.example.yml config/application.yml

test:
  override:
    - RAILS_ENV=test bundle exec rspec -r rspec_junit_formatter --format progress --format RspecJunitFormatter -o $CIRCLE_TEST_REPORTS/rspec/junit.xml --require rails_helper

#deployment:
#  dev:
#    branch: develop
#    heroku:
#      appname: pragma-rails-starter-dev
#
#  staging:
#    branch: staging
#    heroku:
#      appname: pragma-rails-starter-staging
#
#  production:
#    branch: master
#    heroku:
#      appname: pragma-rails-starter-production
